╔════════════════════════════════════════════════════════════════════╗
║     HEADER & FOOTER OPTIMIZATION - QUICK REFERENCE GUIDE             ║
╚════════════════════════════════════════════════════════════════════╝

📂 PROJECT STRUCTURE AFTER OPTIMIZATION:
─────────────────────────────────────────

new_site/
├── includes/
│   ├── header.html          ← Common header (loaded by all pages)
│   └── footer.html          ← Common footer (loaded by all pages)
├── css/
│   └── style.css
├── js/
│   └── script.js            ← Contains loadHeader() & loadFooter()
├── pages/
│   ├── contact.html         ← Has #header-container & #footer-container
│   ├── news.html            ← Has #header-container & #footer-container
│   ├── opinion.html         ← Has #header-container & #footer-container
│   └── vilay.html           ← Has #header-container & #footer-container
└── index.html               ← Has #header-container & #footer-container


🔄 HOW PAGES LOAD HEADER & FOOTER:
────────────────────────────────────

1. HTML Page Loads
   ↓
2. jQuery loads from CDN
   ↓
3. script.js executes
   ↓
4. $(document).ready() triggers
   ↓
5. $.when(loadHeader(), loadFooter()).done()
   ├── Detects if page is in /pages/ or root
   ├── Loads appropriate HTML files
   └── Updates links with correct paths
   ↓
6. initializeComponents() runs
   ├── Tooltips initialized
   ├── Smooth scrolling enabled
   ├── Form validation enabled
   └── Other features initialized


⚙️ KEY JQUERY FUNCTIONS:
────────────────────────

loadHeader()
└─ Loads /includes/header.html
   └─ Calls setActiveNavLink()
      └─ Maps current page to active nav item

loadFooter()
└─ Loads /includes/footer.html
   └─ Calls updateFooterLinks()
      └─ Updates link paths based on location

setActiveNavLink()
└─ Detects current page
   └─ Marks matching nav link as active
   └─ Updates all nav link href attributes

updateFooterLinks()
└─ Updates footer link href attributes
   └─ Handles path differences between root and /pages/


🎨 DATA ATTRIBUTES USED:
─────────────────────────

Each navigation link uses:
<a href="#" data-page="home">Home</a>
<a href="#" data-page="vilay">Vilay</a>
<a href="#" data-page="opinion">Opinion</a>
<a href="#" data-page="news">News</a>
<a href="#" data-page="contact">Contact</a>

Page Mapping:
    'index.html'    → 'home'
    'index_2.html'  → 'home'
    'vilay.html'    → 'vilay'
    'opinion.html'  → 'opinion'
    'news.html'     → 'news'
    'contact.html'  → 'contact'


📍 CONTAINER DIVS IN HTML:
────────────────────────────

Header Container:
<body>
    <div id="header-container"></div>  ← Filled by jQuery
    ...

Footer Container:
...
    <div id="footer-container"></div>  ← Filled by jQuery
    <script src="..."></script>
</body>


🔗 PATH HANDLING LOGIC:
────────────────────────

CURRENT LOCATION: /new_site/
├── Home link → index.html
├── Vilay link → pages/vilay.html
├── Opinion link → pages/opinion.html
├── News link → pages/news.html
└── Contact link → pages/contact.html

CURRENT LOCATION: /new_site/pages/
├── Home link → ../index.html
├── Vilay link → vilay.html
├── Opinion link → opinion.html
├── News link → news.html
└── Contact link → contact.html


✅ TESTING CHECKLIST:
──────────────────────
□ Open index.html in browser
  └─ Verify header loads with navigation
  └─ Verify footer loads with quick links
  └─ Click each nav link (should navigate correctly)
  
□ Open pages/news.html
  └─ Verify header appears (with News as active)
  └─ Verify footer appears (footer links work)
  └─ Click Home in header (should go to index.html)
  
□ Open pages/contact.html
  └─ Verify header appears (with Contact as active)
  └─ Verify footer appears
  └─ Test navigation to other pages
  
□ Check browser console
  └─ No 404 errors for missing files
  └─ No JavaScript errors


📝 TO UPDATE HEADER GLOBALLY:
──────────────────────────────
1. Edit: /includes/header.html
2. Changes auto-apply to all pages
3. No need to modify individual page files


📝 TO UPDATE FOOTER GLOBALLY:
──────────────────────────────
1. Edit: /includes/footer.html
2. Changes auto-apply to all pages
3. No need to modify individual page files


⚡ PERFORMANCE NOTES:
──────────────────────
- Header & footer load via AJAX (parallel loading)
- DOM ready when script executes
- No render-blocking operations
- Minimal network overhead (small HTML files)
- jQuery handles cross-browser compatibility


🐛 TROUBLESHOOTING:
────────────────────
If header/footer don't load:
1. Check browser console for errors
2. Verify /includes/ folder exists
3. Verify jQuery is loaded before script.js
4. Check network tab in DevTools for 404s
5. Ensure proper file paths in loadHeader() & loadFooter()

If nav links don't work:
1. Verify data-page attributes match page mapping
2. Check updateNavLinks() is being called
3. Verify window.location.pathname detection
